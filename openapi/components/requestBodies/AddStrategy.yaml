content:
  application/json:
    schema:
      required:
        - strategy
        - accountShopItemId
      properties:
        accountShopItemId:
          type: string
          format: uuid
        strategy:
          $ref: ../schemas/Strategy.yaml