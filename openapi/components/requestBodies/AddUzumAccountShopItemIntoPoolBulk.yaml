content:
  application/json:
    schema:
      required:
        - shopId
        - shopItemIds
      properties:
        shopId:
          type: string
          format: uuid
        shopItemIds:
          type: array
          items:
            $ref: '../schemas/ShopItemId.yaml'
